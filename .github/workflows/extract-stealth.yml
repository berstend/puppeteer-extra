name: Extract stealth.min.js

on:
  push:
    branches:
      - master
    paths:
      - "packages/puppeteer-extra-plugin-stealth/**"
      - "packages/extract-stealth-evasions/**"
      - ".github/workflows/extract-stealth.yml"

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Sleep for 60 seconds
        uses: jakejarvis/wait-action@master
        with:
          time: "180s"

      - name: Checkout
        uses: actions/checkout@v2

      - name: Extract stealth.min.js
        run: |
          yarn install
          yarn bootstrap
          lerna run build --scope puppeteer-extra --scope puppeteer-extra-plugin --scope puppeteer-extra-plugin-user-preferences --scope extract-stealth-evasions --scope puppeteer-extra-plugin-stealth
          cd packages/extract-stealth-evasions
          node index.js
          cp stealth.min.js ../../

      - name: Commit stealth.min.js
        uses: EndBug/add-and-commit@v4
        with:
          add: "stealth.min.js"
          force: true
          ref: "stealth-js"
          message: "Auto-updated stealth.min.js with newest evasions"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
